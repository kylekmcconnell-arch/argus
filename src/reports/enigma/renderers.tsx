import type { ReportLaneRenderers } from "../shared/reportLaneRendererTypes";

/**
 * Enigma-owned presentation slots. Enigma can add or replace renderers here
 * without changing Kyle, Production, Raw Evidence, or the shared data engine.
 */
export const enigmaReportRenderers: ReportLaneRenderers = {};

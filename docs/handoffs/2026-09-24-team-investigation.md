# Team links and person research repair

Issue #535. Presentation 2026-09-24.1.

The People chapter's full-audit action used the ordinary cached-report callback. It now uses the same fresh background research callback as Connections; the original company report stays open and its evidence stays frozen. The application preserves the parent report's private mode. Shared reports expose no paid audit action.

The save/render paths stripped even a subject_first_party X handle when another enrichment marked identity links model_lead. That explicit first-party handle is now preserved through the saved roster, person graph key and analyst packet. Guessed LinkedIn/developer identities remain withheld. Recorded X profile URLs normalize to actionable canonical links. LinkedIn links require a real profile path on the actual platform host and malformed encodings cannot crash the view.

Candidate X and LinkedIn profiles are clickable as clearly labeled leads, including candidates grouped inside an existing roster card. This does not corroborate the account or change its evidence tier. Reported prior projects attached to a person are visible with source context and no inferred venture outcome or common control. Developer profiles are available on the roster card as well as its evidence view.

The deeper panel includes focused public-web, LinkedIn and X history searches. These are explicitly external searches, not a completed automated investigation. The existing automated person pipeline still requires a resolved X account; this repair does not introduce LinkedIn-only/name-only automated audits. No guessed handle is constructed. Missing sources remain missing.

Validation: full offline quality suite (5,351 passing tests and one existing expected failure), canaries, calibration, all type projects and production build. Report interaction regression proves the fresh callback wins over cached navigation; identity regressions preserve first-party handles while excluding model guesses and deceptive URLs. The historical Altcoinist preview was visually inspected through the People chapter and the expanded Endre Abraham evidence panel. No paid live audit was run.

Rollout uses protected PR checks and a merge to main. Rollback reverts this PR; no database migration or historical evidence mutation is required.

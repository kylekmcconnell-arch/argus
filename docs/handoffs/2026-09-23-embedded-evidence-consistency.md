# Embedded contract evidence consistency

Issue: #525. Route: Argus canonical product repository. Risk: saved-report integrity and presentation.

The embedded contract evidence now uses the report typography without a duplicate subject, risk-ring hero, or Share/Export actions. Saved threat records no longer automatically fetch AI contract reads, market charts, shared ledger counters, or expose supplemental analysis as if it belonged to the saved record. Explicit current-analysis surfaces retain their supplemental tools.

Embedded address-only scans preserve the requested chain on fallback and reject cached results for a different chain/address. Funding connections are described as leads, not proof that several wallets are one operator; unavailable clustering remains unassessed.

Validation: full quality battery passed (5,273 passing tests plus one expected failure, seven canaries, 21 calibration cases, client/server/API types), production build, and desktop/390px browser inspection. Regression tests cover frozen-render network isolation and wrong-chain cache fallback. No paid live enrichment was run.

Acceptance: opening saved contract evidence is network-isolated from supplemental analysis; chapter evidence has no duplicate report hero; fallback scans retain their subject identity. Rollout follows protected-main CI. Rollback is a revert; no data migration or saved-record mutation.

Outstanding: provider coverage, complete launch populations, paid wallet enrichment, and outcome-tested alerting remain tracked in #525. These visual and integrity repairs do not complete those tasks.
